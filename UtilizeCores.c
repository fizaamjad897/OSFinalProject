#include <stdio.h>

int main() {
    int tasks;
    printf("Enter number of tasks to utilize CPU cores: ");
    scanf("%d", &tasks);
    printf("%d", tasks); // Print number of tasks to utilize CPU cores
    return 0;
}
